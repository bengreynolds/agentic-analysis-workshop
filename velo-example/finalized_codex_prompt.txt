Create a small standalone Python project for generating ReachX trajectory-speed figures. Do not modify ReachX source code or ReachX outputs. Use the existing temp/ folder only as a reference for ReachX source code and example outputs, and ensure temp/ is listed in .gitignore and remains untracked.

Before implementing, inspect the relevant ReachX source/output files in temp/ to understand the fixed-cam and legacy output formats. Create a senior-dev-level PLANNING.md first that defines the intended scope, file structure, workspace detection strategy, curated-reach loading strategy, plotting approach, validation checks, assumptions, risks, and staged implementation steps. Report the plan back to the user for iteration/review before making code changes. Do not begin implementation if curated-reach logic or output structure is unclear; pause and request review.

Create a project-level AGENTS.md that follows Codex expectations for repository instructions: it should define how Codex should operate in this project, including minimal bounded changes, no ReachX edits, no raw data modification, no forced completion around flawed assumptions, beginner-readable code, no classes unless unavoidable, no unnecessary tests/temp files, no broad refactors, and smallest-useful validation after changes. Codex supports layered AGENTS.md files for project-specific behavior, so this should live at the standalone project root and constrain work inside this project.

Implement a standalone script that opens a file dialog to select a ReachX session folder, supports fixed-cam and legacy workspace formats, allows manual workspace selection or auto-detection when clear, and stops for review if detection is ambiguous. The script must use curated reaches only, never algorithm-scored reaches. If curated reach data is missing, ambiguous, or inconsistent with the planned method, stop and report the issue instead of falling back to scored data.

The script should let the user plot either one curated reach or all curated reaches from a selected session. For single-reach mode, provide a simple way to choose the reach index or ID after loading curated reach data. For all-reach mode, plot all curated reaches together on one combined figure, not as separate per-reach figures. The 2D figure should plot y position on the plot x-axis and z position on the plot y-axis, color-code speed along the path with a smooth continuous heatmap, include a labeled speed colorbar, label axes with units, and include session/reach info in the title. Also support a live 3D Plotly figure with x/y/z axes shown and an editable/rotatable browser view. Save 2D PNG and PDF outputs to figures/. Save 3D interactive HTML outputs to figures/.

Add a README.md with complete setup and usage instructions because no conda environment exists. Include conda environment creation, dependency installation including Plotly, how to run the script, how the session picker works, workspace options, single-reach vs. all-reach plotting, 2D vs. live 3D plotting, output locations, assumptions, validation checks, and troubleshooting.

Keep the implementation simple and reviewable: plain Python, clear names, short helper functions only when they reduce repeated logic, comments for major steps, no package structure unless necessary, no classes, no unnecessary dependencies, no full-file rewrites, and no broad refactors. After implementation, validate only at the smallest useful level: confirm expected files were created, temp/ remains untracked, raw data was not modified, and the script logic matches PLANNING.md.

Review the task, references, and goals first. Create the markdown planning files before doing any implementation. Report the plan back to me for iteration/review before making code changes. Do not edit or write any Python code until I review and approve the markdown files.
